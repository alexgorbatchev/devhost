{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "check": "bun run --cwd packages/devhost check && bun run --cwd packages/www check",
    "policy:fix:semantic": "bun run typescript-ai-policy-fix-semantic -- ."
  },
  "devDependencies": {
    "@alexgorbatchev/typescript-ai-policy": "^1.0.3",
    "oxfmt": "^0.42.0",
    "oxlint": "^1.57.0"
  }
}
