name = "hello-test-app"
primaryService = "hello"
devtoolsMinimapPosition = "right"
devtoolsPosition = "top-right"

[services.hello]
command = ["bun", "run", "dev"]
cwd = "."
port = 3200
host = "hello.xcv.lol"

[services.logs]
command = ["bun", "run", "log-demo"]
cwd = "."

[services.logs.health]
process = true
